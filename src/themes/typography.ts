/**
 * Typography guidelines for MarkPress (desktop + mobile WeChat).
 *
 * line-height recommendation:
 * - Body text: 1.75 (default) — balances Chinese readability on phone (~375px)
 *   and desktop article width (~680px).
 * - Comfortable range: 1.7 – 1.8
 * - Avoid < 1.6 (cramped on mobile) or > 1.95 (loose, hard to scan).
 *
 * References commonly used by CN content platforms / WeChat long-form:
 * roughly 1.6–1.8 with 15–17px body size.
 */
export const RECOMMENDED_LINE_HEIGHT = 1.75;
export const RECOMMENDED_FONT_SIZE = 16;
